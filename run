#!/bin/sh
set -eu
tool_root=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
repo_root=${BR_PRE_COMMIT_REPO_ROOT:-$(git rev-parse --show-toplevel)}
export BR_PRE_COMMIT_REPO_ROOT="$repo_root"
exec python "$tool_root/src/br_pre_commit/precommit_wrapper.py" "$@"
