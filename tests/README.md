# Tests

This directory owns regression tests for the shared wrapper, configuration,
backup/recovery, ratchet, and skill synchronization code.

Run the suite from the repository root:

```sh
python -m pytest -q
```
